# Changelog

## 1.0.0

- Initial release: warmup reps, graded exercises, gate, and drop cards.
- Optional `before_implement` hook bound to `speckit.gym.gate` to require a
  cleared gate before implementation.
